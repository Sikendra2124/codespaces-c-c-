#include <stdio.h>
int main()
{
    int a, b;
    printf("enter the value of a:");
    scanf("%d", &a);
    printf("enter the value of b:");
    scanf("%d", &b);
    printf("addition of two number=%d", (a + b));
    printf("subtraction of two number=%d", (a - b));
    printf("multiplication of two number=%d", (a * b));
    printf("division of two number=%d", (a + b));
    printf("modulo of two number=%d", (a + b));
    return 0;
}